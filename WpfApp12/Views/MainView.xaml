<Window x:Class="WpfApp12.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp12.Views"
        xmlns:prism="http://prismlibrary.com/"          
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <StackPanel>
        <TextBox Text="{Binding Title, Mode=TwoWay}" Margin="10" Height="30"/>

        <Button Content="Click" Command="{Binding ClickCommand}" Margin="10"/>
        
        <Button Content="Get" Command="{Binding GetCommand}" Margin="10"/>
    </StackPanel>
</Window>
