<Window x:Class="WpfApp12.Views.MainRegionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp12.Views"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="MainRegionView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 导航区域 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="待办列表" Command="{Binding NavigateCommand}" 
                    CommandParameter="TodoListView" Margin="0,0,10,0"/>
            <Button Content="关于" Command="{Binding NavigateCommand}" 
                    CommandParameter="AboutView" Margin="0,0,10,0"/>

            <Button Content="打开对话框" Command="{Binding ShowDialogCommand}" />
        </StackPanel>

        <!-- 内容区域：用于显示不同的View -->
        <ContentControl Grid.Row="1" Margin="10"
                        prism:RegionManager.RegionName="ContentRegion"/>
    </Grid>
</Window>
