<UserControl x:Class="WpfApp13.Views.AddTodoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp13.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:prism="http://prismlibrary.com/"
prism:ViewModelLocator.AutoWireViewModel="True">
    <StackPanel Margin="10">
        <TextBlock Text="添加新待办" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Orientation="Horizontal">
            <TextBox Text="{Binding NewTodoTitle, UpdateSourceTrigger=PropertyChanged}" 
                     Width="300" Height="30" Margin="0,0,10,0" Padding="5"/>
            <Button Content="添加" Command="{Binding AddTodoCommand}" 
                    Width="80" Height="30" Background="#4CAF50" Foreground="White"/>
        </StackPanel>

        <TextBlock Text="{Binding Message}" Foreground="Green" Margin="0,10,0,0" FontSize="14"/>
    </StackPanel>
</UserControl>
