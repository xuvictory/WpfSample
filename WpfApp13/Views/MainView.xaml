<Window x:Class="WpfApp13.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp13.Views"
        mc:Ignorable="d"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="Prism 待办事项" Height="500" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 导航栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" Background="#F0F0F0">
            <Button Content="📋 待办列表" Command="{Binding NavigateCommand}" 
                    CommandParameter="TodoListView" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="➕ 添加待办" Command="{Binding NavigateCommand}" 
                    CommandParameter="AddTodoView" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="ℹ️ 关于" Command="{Binding NavigateCommand}" 
                    CommandParameter="AboutView" Padding="10,5"/>
        </StackPanel>

        <!-- 内容区域 -->
        <ContentControl Grid.Row="1" Margin="10"
                        prism:RegionManager.RegionName="ContentRegion"/>
    </Grid>
</Window>
