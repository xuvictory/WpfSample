<UserControl x:Class="WpfApp13.Views.TodoListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp13.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:converters="clr-namespace:WpfApp13.Converters"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:StrikeThroughConverter x:Key="StrikeThroughConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="待办事项" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <ListBox Grid.Row="1" ItemsSource="{Binding Todos}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <CheckBox IsChecked="{Binding IsCompleted}" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding Title}" 
                                   TextDecorations="{Binding IsCompleted, 
                                       Converter={StaticResource StrikeThroughConverter}}"/>
                        <Button Content="删除" Command="{Binding DataContext.RemoveTodoCommand, 
                                RelativeSource={RelativeSource AncestorType=ListBox}}"
                                CommandParameter="{Binding}" Margin="20,0,0,0"
                                Background="#FF6B6B" Foreground="White" Padding="10,3"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock Grid.Row="2" Text="{Binding StatsText}" Margin="0,10,0,0" FontSize="12" Foreground="Gray"/>
    </Grid>
</UserControl>
